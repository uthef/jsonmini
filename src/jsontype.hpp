#ifndef JSONTYPE_HPP
#define JSONTYPE_HPP

namespace jsonmini {
    enum JsonType {
        JSON_MAP,
        JSON_ARRAY,
        JSON_STRING,
        JSON_NUMBER,
        JSON_BOOLEAN,
        JSON_NULL
    };
}

#endif
